CXX_STD = CXX14
PKG_CXXFLAGS = -DRCPP_PARALLEL_USE_TBB=0 \
               $(shell "${R_HOME}/bin/Rscript.exe" -e "RcppParallel::CxxFlags()")
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) \
           $(shell "${R_HOME}/bin/Rscript.exe" -e "RcppParallel::RcppParallelLibs()")
