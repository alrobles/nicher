# An ellipse parameter object example. Is a list with two elements. \#' @format A list with two elements. One vector and one matrix:

- mu:

  mu parameters of the ellipse

- S:

  Covariance parameters of the ellipse

An ellipse parameter object example. Is a list with two elements. \#'
@format A list with two elements. One vector and one matrix:

- mu:

  mu parameters of the ellipse

- S:

  Covariance parameters of the ellipse

## Usage

``` r
ellipse_par_example
```

## Format

An object of class `ellipse` of length 2.
