% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_optimize_niche.R
\name{benchmark_optimize_niche}
\alias{benchmark_optimize_niche}
\title{Benchmark cpp vs r backends for optimize_niche}
\usage{
benchmark_optimize_niche(
  env_occ,
  env_m = NULL,
  num_starts = 10L,
  likelihood = c("weighted", "presence_only"),
  seed = NULL,
  control = list(),
  ...
)
}
\arguments{
\item{env_occ}{Data frame of presence-point environmental values.}

\item{env_m}{Data frame of background environmental values
(required for \code{likelihood = "weighted"}).}

\item{num_starts}{Integer. Number of Sobol starting points
(default \code{10L}; small to keep the legacy backend fast).}

\item{likelihood}{One of \code{"weighted"} or \code{"presence_only"}.}

\item{seed}{Integer seed (passed to both runs for identical sampling).}

\item{control}{Named list of control parameters
(forwarded to \code{optimize_niche()}).}

\item{...}{Additional arguments forwarded to \code{optimize_niche()}.}
}
\value{
A data frame with one row per backend, columns:
  \describe{
    \item{\code{backend}}{"cpp" or "r"}
    \item{\code{best_loglik}}{Log-likelihood of the best converged start.}
    \item{\code{best_convergence}}{ucminf convergence code of the best.}
    \item{\code{n_converged}}{Count of starts with convergence \%in\% \{1, 2\}.}
    \item{\code{wall_time_sec}}{Total wall-clock seconds for the run.}
  }
}
\description{
\lifecycle{experimental}
}
\details{
Runs \code{\link{optimize_niche}} from the same Sobol starts under both
\code{backend = "cpp"} and \code{backend = "r"} and returns timing /
convergence statistics for direct comparison. This function exists ONLY
to enable side-by-side benchmarking of the new C++ XPtr backend against
the legacy R-objective backend, and will be removed in the next minor
release alongside \code{backend = "r"}.
}
\examples{
\dontrun{
bench <- benchmark_optimize_niche(
  env_occ    = example_env_occ_2d,
  env_m      = example_env_m_2d,
  num_starts = 5L,
  likelihood = "weighted",
  seed       = 1L
)
print(bench)
}
}
