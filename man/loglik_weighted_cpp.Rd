% Generated by roxygen2: do not edit by hand
% Please edit documentation in src/loglik_weighted.cpp
\name{loglik_weighted_cpp}
\alias{loglik_weighted_cpp}
\title{Log-likelihood for the weighted Gaussian niche model (C++ engine)}
\usage{
loglik_weighted_cpp(sam1, sam2, mu, S, weights)
}
\arguments{
\item{sam1}{Numeric matrix of presence points
(rows = observations, columns = variables).}

\item{sam2}{Numeric matrix of background environmental points.}

\item{mu}{Numeric vector of means (optimum) of length p.}

\item{S}{Covariance matrix (p x p).}

\item{weights}{Numeric vector of non-negative weights for the background
points in \code{sam2}. Length must equal \code{nrow(sam2)}.}
}
\value{
Negative log-likelihood value (scalar double).
}
\description{
Computes the weighted presence-only (semi-log-likelihood) for a Gaussian
niche model using Mahalanobis quadratic forms. Background points can be
given non-negative weights so that denser regions of environmental space
contribute proportionally more to the normalizing integral. Weights are
normalized internally so that they sum to one.

When all weights are equal the result is identical to
\code{\link{loglik_presenceonly_cpp}}.
}
\examples{
par <- get_ellip_par(spOccPnts)
## Uniform weights
w <- rep(1, nrow(samMPts))
loglik_weighted_cpp(as.matrix(spOccPnts), as.matrix(samMPts), par$mu, par$S, w)

## Custom weights proportional to row index (toy example)
w <- seq_len(nrow(samMPts))
loglik_weighted_cpp(as.matrix(spOccPnts), as.matrix(samMPts), par$mu, par$S, w)
}
