% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/start_theta_multiple.R
\name{start_theta_multiple}
\alias{start_theta_multiple}
\title{Generate multiple starting points for niche model optimization}
\usage{
start_theta_multiple(
  env_data,
  num_starts = 100,
  quant_vec = c(0.1, 0.5, 0.9),
  method = "sobol",
  skew = FALSE,
  skew_t = FALSE
)
}
\arguments{
\item{env_data}{Environmental data (matrix or data frame). Must be numeric.}

\item{num_starts}{Integer, number of starting points.}

\item{quant_vec}{Quantiles for mu ranges.}

\item{method}{"sobol" (Sobol design) or "uniform".}

\item{skew}{Logical. If \code{TRUE}, append \code{p} skewness parameters
(\code{alpha_1, ..., alpha_p}) to each starting vector with the
default range \code{[-3, 3]}. Use this for the
\code{"skew_normal"} / \code{"skew_normal_weighted"} likelihoods.
Default \code{FALSE}.}

\item{skew_t}{Logical. If \code{TRUE}, additionally append a single
\code{log_r} parameter (default range \code{[log 2, log 100]})
after the alpha block. Use for \code{"skew_t"} /
\code{"skew_t_weighted"}. Implies \code{skew = TRUE}.}
}
\value{
A data frame of dimension num_starts × num_parameters.
}
\description{
Creates a set of starting parameter vectors on the math scale for use with
the log-likelihood functions. Ensures all starting values are strictly
numeric and finite, even if env_data is supplied as a data frame.
}
\examples{
starts <- start_theta_multiple(
  env_data   = example_env_occ_2d,
  num_starts = 5L,
  method     = "uniform"
)
dim(starts)
}
