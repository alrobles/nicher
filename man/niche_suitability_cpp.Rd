% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{niche_suitability_cpp}
\alias{niche_suitability_cpp}
\title{Habitat-suitability kernel (parallel, zero-copy)}
\usage{
niche_suitability_cpp(
  env_dat_vec,
  env_dat_dims,
  mu,
  L_inv,
  return_log = FALSE,
  num_threads = 0L
)
}
\arguments{
\item{env_dat_vec}{Numeric vector. Flat column-major buffer of length
\code{n_loc * p}; entry for location \code{l} variable \code{k} sits
at index \code{l + n_loc * k}.}

\item{env_dat_dims}{Integer vector \code{c(n_loc, p)}.}

\item{mu}{Numeric vector of length \code{p}: niche centroid.}

\item{L_inv}{Numeric matrix \code{p x p}: inverse of the lower
Cholesky factor of \eqn{\Sigma}. Precomputed R-side once.}

\item{return_log}{Logical. If \code{FALSE} (default) returns
suitability in \eqn{(0, 1]}; if \code{TRUE} returns
\eqn{\log S(x) \le 0}.}

\item{num_threads}{Integer. \code{0} (default) leaves
\code{RcppParallel}'s global thread state unchanged.}
}
\value{
Numeric vector of length \code{n_loc}.
}
\description{
Evaluates the standardized multivariate-normal density
\eqn{S(x) = \exp(-\tfrac12 (x-\mu)^\top \Sigma^{-1} (x-\mu))} for each
row of a column-major flat environmental buffer. Uses
\pkg{RcppParallel}'s \code{parallelFor} over locations.
}
\keyword{internal}
