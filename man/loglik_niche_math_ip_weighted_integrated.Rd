% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loglik_niche_math_ip_weighted_integrated.R
\name{loglik_niche_math_ip_weighted_integrated}
\alias{loglik_niche_math_ip_weighted_integrated}
\title{Negative log-likelihood (inverse-probability-weighted normal, integrated C++)}
\usage{
loglik_niche_math_ip_weighted_integrated(
  theta,
  env_occ,
  env_m,
  eta = 1,
  neg = TRUE,
  den_idx = NULL,
  kde_idx = NULL,
  precomp_w_den = NULL
)
}
\arguments{
\item{theta}{Numeric vector of unconstrained parameters (math scale):
\eqn{[\mu, \log\sigma, v]} of length \eqn{2p + p(p-1)/2}.}

\item{env_occ}{Data frame or matrix (\eqn{n \times p}) of environmental
values at presence points.}

\item{env_m}{Data frame or matrix (\eqn{n_m \times p}) of background
environmental values from the accessible area M.}

\item{eta}{Numeric scalar, shape parameter for the LKJ C-vine prior on the
correlation matrix (default 1 = uniform over correlations).}

\item{neg}{Logical. If \code{TRUE} (default), returns the negative
log-likelihood (suitable for minimisation).}

\item{den_idx}{Integer vector of 1-based row indices for the denominator
subsample. If \code{NULL} (default), uses all rows of \code{env_m}.}

\item{kde_idx}{Integer vector of 1-based row indices for the KDE reference
subsample. If \code{NULL}, uses all rows of \code{env_m}.}

\item{precomp_w_den}{Numeric vector of precomputed KDE weights for the
denominator points (must match \code{den_idx} in length). If provided,
KDE for the denominator is not recomputed.}
}
\value{
Scalar numeric: the (negative) log-likelihood.
}
\description{
Low-level C++ bridge for the inverse-probability-weighted (IPW) normal
niche model. Computes KDE weights and Mahalanobis distances entirely
in C++ to minimise R overhead. This is the workhorse called by
\code{\link{loglik_niche_math_ip_weighted}}.
}
\details{
The objective function is described in detail in
\code{\link{loglik_niche_math_ip_weighted}}.

This function was formerly called
\code{loglik_niche_math_kde_bias_corrected_integrated}.
}
\examples{
\donttest{
theta <- start_theta(example_env_occ_2d)
ll <- loglik_niche_math_ip_weighted_integrated(
  theta   = theta,
  env_occ = example_env_occ_2d,
  env_m   = example_env_m_2d,
  eta     = 1,
  neg     = TRUE
)
print(ll)
}

}
\seealso{
[loglik_niche_math_ip_weighted()] for the user-facing
  wrapper with automatic subsampling.
}
