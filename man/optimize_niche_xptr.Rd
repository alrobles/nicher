% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_niche_xptr.R
\name{optimize_niche_xptr}
\alias{optimize_niche_xptr}
\title{Optimize niche model using a compiled XPtr backend}
\usage{
optimize_niche_xptr(
  start = NULL,
  xptr,
  control = ucminfcpp::ucminf_control(grad = "central", gradstep = c(1e-06, 1e-08),
    maxeval = 200),
  multi_start = FALSE
)
}
\arguments{
\item{start}{Numeric vector (single start) or list of numeric vectors (multi-start).}

\item{xptr}{External pointer created by create_niche_obj_ptr().}

\item{control}{List of control parameters for ucminfcpp::ucminf_control().}

\item{multi_start}{Logical; TRUE if multiple starts are supplied.}
}
\value{
A list with:
  \itemize{
    \item par — best parameter vector
    \item value — negative log-likelihood
    \item conv — convergence code
    \item all_results — (multi-start only) table of all runs
  }
}
\description{
Runs ucminfcpp::ucminf_xptr() over a compiled C++ objective function
created by create_niche_obj_ptr(). Supports single-start and multi-start
optimization. Ensures that all starting vectors are numeric doubles and finite.
}
\examples{
\dontrun{
occ    <- as.matrix(example_env_occ_3d)
M      <- as.matrix(example_env_m_3d)
theta0 <- start_theta(example_env_occ_3d)
xptr   <- create_niche_obj_ptr(
  env_occ    = occ,
  env_m      = M,
  likelihood = "unweighted",
  eta        = 1
)
res <- optimize_niche_xptr(start = theta0, xptr = xptr)
res$value
}
}
